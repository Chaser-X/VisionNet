namespace VisionNet.Controls
{
    public enum SurfaceMode
    {
        PointCloud = 1,
        Mesh = 2,
    }

    public enum SurfaceColorMode
    {
        Color,
        Intensity,
        ColorWithIntensity,
    }
}
