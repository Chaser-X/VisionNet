using VisionNet.DataType;

namespace VisionNet
{
    /// <summary>
    /// Segmentation and clustering operations (Euclidean, region growing, RANSAC primitives).
    /// </summary>
    public static partial class VisionOperator
    {
        // ── 预留扩展 ──────────────────────────────────────────────────────────
        // EuclideanCluster    欧式距离聚类
        // RegionGrowing       区域生长分割
        // PlaneSegmentation   平面分割（RANSAC）
        // CylinderSegmentation 圆柱分割
    }
}
