using VisionNet.DataType;

namespace VisionNet
{
    /// <summary>
    /// Defect and object detection operations (surface inspection, template matching).
    /// </summary>
    public static partial class VisionOperator
    {
        // ── 预留扩展 ──────────────────────────────────────────────────────────
        // DetectDefects       表面缺陷检测
        // DetectObject        目标识别
        // CompareWithTemplate 与模板比较
    }
}
